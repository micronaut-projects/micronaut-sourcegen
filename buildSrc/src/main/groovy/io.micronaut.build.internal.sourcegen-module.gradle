plugins {
    id("io.micronaut.build.internal.sourcegen-base")
    id("io.micronaut.build.internal.module")
    id("io.micronaut.build.internal.sourcegen-nullaway")
}
tasks.test {
    useJUnitPlatform()
}
micronautBuild {
    descriptor {
        parentModuleId = "io.micronaut.sourcegen:micronaut-sourcegen-annotations"
    }
}
