If you annotate a type with `@Builder`, a `PersonBuilder` class is generated at compilation-time.

snippet::io.micronaut.sourcegen.docs.builder.Person[tags="clazz", indent=0, project-base="test-suite", source="main"]

You can use a https://blogs.oracle.com/javamagazine/post/exploring-joshua-blochs-builder-design-pattern-in-java[builder pattern] to create a `Person` instance:

snippet::io.micronaut.sourcegen.docs.builder.PersonBuilderTest[tags="test", indent=0, project-base="test-suite"]

[.lang-python]
NOTE: The generated types have no Python module yet (the Python compiler generates the modules before the sourcegen visitors run): load them with `java.type("micronaut.sourcegen.docs.builder.PersonBuilder")` until the Python source generator ships.

NOTE: The https://github.com/micronaut-projects/micronaut-sourcegen/blob/master/sourcegen-generator/src/main/java/io/micronaut/sourcegen/generator/visitors/BuilderAnnotationVisitor.java[BuilderAnnotationVisitor] is an example of how to use the Micronaut SourceGen API.
