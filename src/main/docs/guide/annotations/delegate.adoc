If you annotate an interface with `@Delegate`, a `<type>Delegate` abstract class is generated at compilation-time.

snippet::io.micronaut.sourcegen.docs.delegate.Worker[tags="clazz", indent=0, project-base="test-suite", source="main", languages="java,kotlin"]

snippet::io.micronaut.sourcegen.docs.delegate.SimpleWorker[tags="clazz", indent=0, project-base="test-suite", source="main", languages="java,kotlin"]

You can use a delegate pattern to change the behavior of a Worker delegatee:

snippet::io.micronaut.sourcegen.docs.delegate.OvertimeWorker[tags="clazz", indent=0, project-base="test-suite", source="main", languages="java,kotlin"]

NOTE: The delegate extends the generated `WorkerDelegate` abstract class, which Python classes cannot do (a Python class cannot extend a Java class) and Groovy classes cannot resolve in the same compilation, so this example is only shown for Java and Kotlin.

The delegate will delegate the behavior to inner object except for changed methods:

snippet::io.micronaut.sourcegen.docs.delegate.DelegateWorkerTest[tags="test", indent=0, project-base="test-suite", languages="java,kotlin"]
