To use a builder pattern with types that are using inheritance you annotate a bean with `@SuperBuilder`:

snippet::io.micronaut.sourcegen.docs.superbuilder.Animal[tags="clazz", indent=0, project-base="test-suite", source="main", languages="java,groovy,python"]

snippet::io.micronaut.sourcegen.docs.superbuilder.Cat[tags="clazz", indent=0, project-base="test-suite", source="main", languages="java,groovy,python"]

snippet::io.micronaut.sourcegen.docs.superbuilder.Dog[tags="clazz", indent=0, project-base="test-suite", source="main", languages="java,groovy,python"]

[.lang-kotlin]
NOTE: `@SuperBuilder` is not supported for Kotlin classes yet: the Kotlin source generator fails to generate the abstract builder of the super-class (`doesn't have a field: name`). Use `@Builder` on each class instead.

For every class annotated with `@SuperBuilder` there going to be two builders generated at compilation-time, an abstract one intended to be inherited and second one to constructor the bean.

In the previous example five classes are generated:

- `AbstractAnimalSuperBuilder`
- `AbstractCatSuperBuilder`
- `CatSuperBuilder`
- `AbstractDogSuperBuilder`
- `DogSuperBuilder`

NOTE: `@SuperBuilder` requires every super-class to be annotated with `@SuperBuilder`

You can use a builder to create an instance:

snippet::io.micronaut.sourcegen.docs.superbuilder.AnimalSuperBuilderTest[tags="test", indent=0, project-base="test-suite", languages="java,groovy,python"]

[.lang-python]
NOTE: The no-argument constructor of the generated builder is package-private, so Python code uses the public static `builder()` factory method.
