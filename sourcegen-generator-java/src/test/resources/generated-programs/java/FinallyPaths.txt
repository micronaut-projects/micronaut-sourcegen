package test;

import java.util.concurrent.atomic.AtomicInteger;

public class FinallyPaths {
  public int call(AtomicInteger counter, boolean fail) {
    try {
      if (fail) {
        throw new java.lang.IllegalStateException();
      }
      return 7;
    } finally {
      counter.incrementAndGet();
    }
  }
}
